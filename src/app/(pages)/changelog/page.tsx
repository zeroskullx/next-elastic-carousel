import clsx from 'clsx'
import { Wrapper } from '../../components/layout/Wrapper'

import s from './styles.module.scss'

export default function Changelog() {
  return <Wrapper mainChildren={'Changelog'} sectorNav={'sector changelog'} />
}
